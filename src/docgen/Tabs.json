  {
    "component": "Tabs",
    "source": "src/js/Tabs/Tabs.js",
    "description": "The `Tabs` component is a container for the `Tab` component. It will\nmanage selecting the current active tab and allow the tab content\nto be swiped to change.\n\n> Tabs should not be used for indicating navigation.",
    "props": {
      "style": {
        "type": {
          "name": "object"
        },
        "required": false,
        "description": "An optional style to apply.",
        "defaultValue": {
          "value": "{}",
          "computed": false
        }
      },
      "className": {
        "type": {
          "name": "string"
        },
        "required": false,
        "description": "An optional className to apply."
      },
      "children": {
        "type": {
          "name": "arrayOf",
          "value": {
            "name": "node"
          }
        },
        "required": true,
        "description": "The list of tabs to manage."
      },
      "initialActiveTabIndex": {
        "type": {
          "name": "number"
        },
        "required": true,
        "description": "The initial index of the tab that is active.",
        "defaultValue": {
          "value": "0",
          "computed": false
        }
      },
      "activeTabIndex": {
        "type": {
          "name": "number"
        },
        "required": false,
        "description": "The active tab index if you want to control the tabs yourself."
      },
      "onChange": {
        "type": {
          "name": "func"
        },
        "required": false,
        "description": "An optional function to call when the tab is changed. The next active\ntab index and the change event will be given.\n\n`onChange(tabIndex, event)`."
      },
      "primary": {
        "type": {
          "name": "bool"
        },
        "required": false,
        "description": "Boolean if the tabs should be styled with the primary color.",
        "defaultValue": {
          "value": "true",
          "computed": false
        }
      },
      "fixedWidth": {
        "type": {
          "name": "bool"
        },
        "required": false,
        "description": "Boolean if the tabs are fixed width. This means that all of them will\nbe the same size.",
        "defaultValue": {
          "value": "false",
          "computed": false
        }
      },
      "centered": {
        "type": {
          "name": "bool"
        },
        "required": false,
        "description": "Boolean if the tabs should be centered so there is extra whitespace\naround the tabs.",
        "defaultValue": {
          "value": "false",
          "computed": false
        }
      }
    }
  }
