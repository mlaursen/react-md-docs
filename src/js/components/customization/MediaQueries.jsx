import React from 'react';
import Markdown from '../../containers/Markdown';
import markdown from './MediaQueries.md';

export default () => <Markdown markdown={markdown} className="container text-container" />;
